You are an expert Python developer helping build a
Rheumatoid Arthritis (RA) Nutrition and Lifestyle App.

Weeks 1–5 are complete. Week 6 mission is to:
1. Add missing ingredients system (migration 0006)
2. Build custom meal builder endpoints
3. Update rule engine with pain-driven meal mode
4. Review and lock auth flow
5. Lock all API contracts before Week 7 frontend

Do NOT build the React frontend — Week 7.
Write and save all code to disk. No pseudocode.

═══════════════════════════════════════════════════════════
CONTEXT FROM WEEKS 1–5
═══════════════════════════════════════════════════════════

Stack:
- Backend: FastAPI (Python)
- Database: PostgreSQL — Docker service name "db"
- ORM: SQLAlchemy 2.0, Alembic migrations
- Validation: Pydantic v2
- DB connection: os.getenv("DATABASE_URL")
- Session: existing get_db_session() from app/db/session.py
  (async generator — never context manager)

Migration chain (DO NOT break this order):
  0001_initial_schema
  0002_add_ai_score_to_foods
  0003_food_source
  0004_meal_library
  0005_food_log_enhancements
  0006 → next migration this week

DB state:
- static.foods: 50+ foods, all verified
- static.meals: 100 curated meals
- static.meal_ingredients: 369 rows
- tracking.custom_meals: empty, tables exist
- tracking.custom_meal_ingredients: empty
- tracking.missing_ingredients: does not exist yet
- intelligence.recommendation_logs: live
- 52 tests currently passing

Rule engine current behaviour (Week 5):
  Priority 1: Safety — allergy hard blocks
  Priority 2: Medication — medication filter
  Priority 3: Flare — reorder flare meals first
  Priority 4: Nutrition — daily gap boosting
  Priority 5: Preference — vegetarian, cuisine
  Priority 6: AI score — anti-inflammatory ranking
  Priority 7: Variety — penalty for recent meals

Pain-driven mode decision (new this week):
  pain_score 0–3 OR no symptom log today
    → mode: normal
    → show top 5 by nutrition + AI score

  pain_score 4–5 OR flare_level mild
    → mode: mixed
    → show 2 flare_friendly meals first
    → then 3 best nutritional meals

  pain_score 6–10 OR flare_level
    moderate/severe
    → mode: flare_only
    → show only is_flare_friendly = true
    → ordered by anti_inflammatory_score DESC

Food log source rules (confirmed Week 5):
  curated_recommendation → exact meal values
  custom_meal → raw USDA values stored
  manual_log → as-is from static.foods

Missing ingredient states (new this week):
  missing_completely → name only, no values
  user_entered → name + nutrient values
  under_review → admin picked it up
  added → now in static.foods
  rejected → not a real food item

═══════════════════════════════════════════════════════════
WEEK 6 MISSION
═══════════════════════════════════════════════════════════

Complete every task fully before moving to next.
Write and save all files. No pseudocode.

───────────────────────────────────────────────────────────
TASK 1 — MIGRATION 0006
───────────────────────────────────────────────────────────

Write migration:
alembic/versions/0006_missing_ingredients_and_fixes.py

PART A — Create tracking.missing_ingredients:

  id UUID PK default gen_random_uuid()
  user_id UUID FK → core.users.id CASCADE
  ingredient_name TEXT NOT NULL
  status VARCHAR(20) default 'missing_completely'
    values: missing_completely, user_entered,
            under_review, added, rejected

  submitted_calories NUMERIC(8,2) nullable
  submitted_protein_g NUMERIC(6,2) nullable
  submitted_carbs_g NUMERIC(6,2) nullable
  submitted_fat_g NUMERIC(6,2) nullable
  submitted_fiber_g NUMERIC(6,2) nullable
  submitted_sugar_g NUMERIC(6,2) nullable
  submitted_sodium_mg NUMERIC(8,2) nullable
  submitted_source TEXT nullable
    user notes like "from packet label"

  reported_count INTEGER default 1
    increments when multiple users request
    the same ingredient

  admin_notes TEXT nullable
  reviewed_by TEXT nullable
  reviewed_at TIMESTAMPTZ nullable

  food_id UUID nullable
    FK → static.foods.id ON DELETE SET NULL
    populated when admin adds it to static.foods

  created_at TIMESTAMPTZ default now()
  updated_at TIMESTAMPTZ default now()

  UNIQUE constraint on:
    (ingredient_name) case-insensitive
    use: UNIQUE (LOWER(ingredient_name))
    so duplicates increment reported_count
    instead of creating new rows

PART B — Add to static.foods:
  needs_review BOOLEAN default false
    true when food was user-submitted
    and not yet admin-verified

PART C — Add to intelligence.recommendation_logs:
  recommendation_mode VARCHAR(20) nullable
    values: normal, mixed, flare_only

Run migration:
  alembic upgrade head

Verify:
  SELECT table_name FROM information_schema.tables
  WHERE table_schema = 'tracking'
  ORDER BY table_name;
  → expect missing_ingredients in list

  SELECT column_name FROM information_schema.columns
  WHERE table_schema = 'static'
  AND table_name = 'foods'
  AND column_name = 'needs_review';
  → expect 1 row

Print: ✓ TASK 1 COMPLETE

───────────────────────────────────────────────────────────
TASK 2 — UPDATE RULE ENGINE: PAIN-DRIVEN MEAL MODE
───────────────────────────────────────────────────────────

Update app/services/rule_engine.py

Replace current flare reorder step (Priority 3)
with pain-driven mode logic:

async def get_recommendation_mode(
    db, user_id: UUID
) -> str:
  Query tracking.symptom_logs for today
  Get most recent pain_score and flare_level

  if no symptom log today:
    return "normal"

  if pain_score >= 6 OR
     flare_level in (moderate, severe):
    return "flare_only"

  if pain_score >= 4 OR flare_level == mild:
    return "mixed"

  return "normal"

Updated Priority 3 — pain-driven mode:

  mode = await get_recommendation_mode(db, user_id)

  if mode == "flare_only":
    candidates = [m for m in candidates
                  if m.is_flare_friendly]
    if len(candidates) == 0:
      [DESIGN DECISION: fallback to full pool
       with warning if no flare meals match
       after allergy + preference filters]
    explanation_prefix = "You reported pain today.
      These meals are gentle, easy to prepare,
      and anti-inflammatory."

  if mode == "mixed":
    flare_meals = [m for m in candidates
                   if m.is_flare_friendly]
    normal_meals = [m for m in candidates
                    if not m.is_flare_friendly]
    candidates = flare_meals[:2] + normal_meals[:3]
    explanation_prefix = "You reported some
      discomfort today. We have prioritised
      gentle meals while keeping your
      nutrition balanced."

  if mode == "normal":
    candidates = candidates
    explanation_prefix = ""

Add recommendation_mode to RecommendationResult:
  recommendation_mode: str

Save recommendation_mode to
intelligence.recommendation_logs

Add flare reorder verification test:
tests/services/test_rule_engine.py

  New test: mixed mode produces correct order
    Setup: 3 flare_friendly meals +
           5 non-flare meals in fixture
    Call: get_next_meal_recommendation
          with pain_score = 5
    Assert:
      result[0].is_flare_friendly = True
      result[1].is_flare_friendly = True
      result[2].is_flare_friendly = False
      recommendation_mode = "mixed"

  New test: flare_only returns only flare meals
    Assert: all returned meals
            is_flare_friendly = True
    Assert: recommendation_mode = "flare_only"

  New test: normal mode returns no filter
    Assert: recommendation_mode = "normal"
    Assert: non-flare meals present in results

Print: ✓ TASK 2 COMPLETE

───────────────────────────────────────────────────────────
TASK 3 — AUTH FLOW REVIEW AND LOCK
───────────────────────────────────────────────────────────

Review and fix app/core/security.py and
app/routers/auth.py before frontend binds.

Endpoints to verify exist and work correctly:

POST /api/v1/auth/register
  Body: email, password, name
  Validation:
    email must be valid format
    password minimum 8 characters
    email must be unique → 409 if duplicate
  Response: UserResponse (no password fields)
  Auto-create: user_preferences row on register

POST /api/v1/auth/login
  Body: email, password
  Response:
    access_token (JWT, 30 min expiry)
    refresh_token (JWT, 7 day expiry)
    token_type: "bearer"
    user: UserResponse
  Store: refresh_token_hash in
         core.authentication_sessions

POST /api/v1/auth/refresh
  Body: refresh_token
  Validation:
    verify refresh_token signature
    verify not expired
    verify exists in authentication_sessions
  Response:
    new access_token
    new refresh_token (rotate)
    old refresh_token invalidated in DB

POST /api/v1/auth/logout
  Auth: required
  Logic: delete current session from
         authentication_sessions
  Response: { status: "logged_out" }

GET /api/v1/auth/me
  Auth: required
  Response: UserResponse with preferences

PUT /api/v1/auth/me
  Auth: required
  Body: name, height, weight (optional fields)
  Response: updated UserResponse

POST /api/v1/auth/change-password
  Auth: required
  Body: current_password, new_password
  Validation:
    verify current_password is correct
    new_password minimum 8 characters
  Response: { status: "password_changed" }

Auth security rules to verify:
  - All protected endpoints return 401
    with no token or expired token
  - All protected endpoints return 401
    with invalid token signature
  - Refresh token rotation prevents reuse
    of old refresh tokens
  - Password stored as bcrypt hash only
  - No password fields ever in any response
  - account_deletion_requests only one
    pending per user at a time

Write tests: tests/routers/test_auth.py
  - POST /register creates user + preferences
  - POST /register duplicate email → 409
  - POST /register weak password → 422
  - POST /login correct credentials → tokens
  - POST /login wrong password → 401
  - POST /refresh valid token → new tokens
  - POST /refresh expired token → 401
  - POST /refresh reused token → 401
  - POST /logout invalidates session
  - GET /me returns user without password
  - PUT /me updates fields correctly
  - POST /change-password works correctly
  - POST /change-password wrong current → 401

Print: ✓ TASK 3 COMPLETE

───────────────────────────────────────────────────────────
TASK 4 — MISSING INGREDIENTS SYSTEM
───────────────────────────────────────────────────────────

Write: app/services/ingredient_service.py

Functions:

search_or_flag_ingredient(
    db, user_id, ingredient_name, submitted_values
) → IngredientSearchResult

  Step 1: search static.foods
    SELECT * FROM static.foods
    WHERE name ILIKE %ingredient_name%
    LIMIT 5
    if found → return matches, done

  Step 2: not found → check missing_ingredients
    if exists with status not in
    (added, rejected):
      increment reported_count
      return existing record

  Step 3: create missing_ingredients row
    if submitted_values all null:
      status = missing_completely
    else:
      status = user_entered
      needs_review = true
    return new record

create_missing_ingredient(
    db, user_id, data
) → MissingIngredientResponse

get_missing_ingredients(
    db, status_filter, limit, offset
) → list[MissingIngredientResponse]
  ordered by reported_count DESC
  then by status (user_entered first)

update_missing_ingredient(
    db, ingredient_id, admin_data
) → MissingIngredientResponse

Write router: app/routers/ingredients.py

POST /api/v1/ingredients/search
  Auth: required
  Body:
    ingredient_name: str
  Response: IngredientSearchResult
    found: bool
    matches: list[FoodResponse] (if found)
    missing_record: MissingIngredientResponse | null

POST /api/v1/ingredients/missing
  Auth: required
  Body:
    ingredient_name: str (required)
    submitted_calories: float | null
    submitted_protein_g: float | null
    submitted_carbs_g: float | null
    submitted_fat_g: float | null
    submitted_fiber_g: float | null
    submitted_sugar_g: float | null
    submitted_sodium_mg: float | null
    submitted_source: str | null
  Logic:
    if all submitted_* are null:
      status = missing_completely
      message = "Added to our review list.
        We will include it soon."
    else:
      status = user_entered
      message = "Thanks. We will verify
        and add this ingredient shortly."
    if ingredient_name already exists
    (case-insensitive) with status not in
    (added, rejected):
      increment reported_count only
      do not create duplicate
  Response:
    status: str
    message: str
    reported_count: int

GET /api/v1/ingredients/missing
  Auth: admin required
  Query params:
    status: optional filter
    limit: default 20
    offset: default 0
  Response: paginated list ordered by
    reported_count DESC,
    user_entered before missing_completely

PATCH /api/v1/ingredients/missing/{id}
  Auth: admin required
  Body:
    status: str
    admin_notes: str | null
    food_id: UUID | null
  Logic:
    update status, admin_notes, food_id
    set reviewed_at = now()
    set reviewed_by = admin user email
  Response: MissingIngredientResponse

Create schemas: app/schemas/ingredient.py
  MissingIngredientCreate
  MissingIngredientResponse
  IngredientSearchResult:
    found: bool
    matches: list[FoodResponse]
    missing_record: MissingIngredientResponse | null
    suggestion_message: str | null

Register router in app/main.py

IMPORTANT USER EXPERIENCE RULE:
  Never block user from building their meal
  because an ingredient is missing.
  Always return a clear message and let
  the user continue without it.
  Missing ingredient excluded from score
  with note: "Jackfruit excluded from score
  — not in database yet. We have noted it
  for review."

Write unit tests:
tests/services/test_ingredient_service.py
  - search found returns food matches
  - search not found creates missing record
  - search not found status missing_completely
    when no values submitted
  - search not found status user_entered
    when values submitted
  - duplicate request increments reported_count
  - admin can update status to added
  - admin update populates food_id

Print: ✓ TASK 4 COMPLETE

───────────────────────────────────────────────────────────
TASK 5 — CUSTOM MEAL BUILDER ENDPOINTS
───────────────────────────────────────────────────────────

Write: app/services/custom_meal_service.py

Functions:

calculate_custom_meal(
    ingredients: list[CustomMealIngredientInput]
) → CustomMealNutrients
  Pure function — no DB write
  For each ingredient:
    fetch food from static.foods by food_id
    total_X += food.X * portion_g / 100
  Calculate anti_inflammatory_score:
    weighted average of ingredient scores
    weight = portion_g / total_portion_g
  Return CustomMealNutrients

create_custom_meal(
    db, user_id, data
) → CustomMealResponse
  Calculate nutrients from ingredients
  Set is_vegetarian automatically:
    true if no ingredient food has
    category protein or fish with
    meat/fish keyword in name
  Set is_flare_friendly automatically:
    true if anti_inflammatory_score >= 6.0
    AND all ingredients are soft/easy foods
    (no hard raw vegetables like carrots)
  Assign tags automatically
  Insert tracking.custom_meals
  Insert tracking.custom_meal_ingredients
  in one transaction

get_custom_meals(
    db, user_id, limit, offset
) → list[CustomMealResponse]

get_custom_meal_by_id(
    db, user_id, meal_id
) → CustomMealResponse

delete_custom_meal(
    db, user_id, meal_id
) → None

Write router: app/routers/custom_meals.py

POST /api/v1/meals/custom/calculate
  Auth: required
  Body: CustomMealCalculateRequest
    name: str
    meal_type: str
    ingredients: list[CustomMealIngredientInput]
      food_id: UUID
      portion_g: float
      cooking_state: str
  Response: CustomMealNutrients
    total_calories: float
    total_protein_g: float
    total_carbs_g: float
    total_fat_g: float
    total_fiber_g: float
    total_sugar_g: float
    total_sodium_mg: float
    total_omega3_g: float
    anti_inflammatory_score: float
    is_vegetarian: bool
    is_flare_friendly: bool
    tags: list[str]
    missing_ingredients: list[str]
      names of ingredients excluded
      because food_id not found
    score_note: str | null
      "Score excludes 1 ingredient
       not yet in our database"
  NO DB WRITE — pure calculation only
  Use: live preview while user builds meal

POST /api/v1/meals/custom
  Auth: required
  Body: CustomMealCreateRequest
    name: str
    meal_type: str
    ingredients: list[CustomMealIngredientInput]
    missing_ingredient_names: list[str]
      names user typed that were not in DB
      these get logged to missing_ingredients
  Response: CustomMealResponse
  Logic:
    calculate nutrients
    save to tracking.custom_meals
    save to tracking.custom_meal_ingredients
    for each missing_ingredient_name:
      POST to missing_ingredients with
      status = missing_completely
      so they get reviewed later

GET /api/v1/meals/custom
  Auth: required
  Query params: limit default 20, offset 0
  Response: paginated list of user's
    custom meals
  Only returns meals owned by current user

GET /api/v1/meals/custom/{meal_id}
  Auth: required
  Response: full CustomMealResponse
    with ingredient list
  404 if not found or not owned by user

DELETE /api/v1/meals/custom/{meal_id}
  Auth: required
  Response: { status: "deleted" }
  404 if not found or not owned by user

ROUTE ORDER — register in this exact order:
  1. /meals/custom/calculate
  2. /meals/custom (POST + GET)
  3. /meals/custom/{meal_id}

Update app/routers/logs.py:
  POST /api/v1/logs/food
  Add custom_meal log_source handling:
    if log_source = custom_meal:
      fetch custom_meal from
        tracking.custom_meals
      store raw nutrient values
        (from ingredient foods in static.foods)
      populate custom_meal_id
      populate display_calories =
        calories * conversion_factor
      populate display_protein_g =
        protein_g * conversion_factor
      populate cooking_state_at_log
      populate conversion_factor_at_log

Create schemas: app/schemas/custom_meal.py
  CustomMealIngredientInput:
    food_id: UUID
    portion_g: float
    cooking_state: str default "cooked"

  CustomMealNutrients:
    all totals, score, flags, tags,
    missing_ingredients, score_note

  CustomMealCreate: name, meal_type,
    ingredients, missing_ingredient_names

  CustomMealIngredientResponse:
    food_id, food_name, portion_g,
    cooking_state, display_note,
    calories_contribution: float
    protein_contribution: float

  CustomMealResponse:
    id, user_id, name, meal_type,
    all nutrient totals, score, flags,
    tags, ingredients, created_at

Register custom_meals router in app/main.py

Write unit tests:
tests/services/test_custom_meal_service.py
  - calculate returns correct nutrient totals
  - calculate weighted score correct
  - calculate is_vegetarian true for
    plant-based ingredients
  - calculate is_vegetarian false when
    chicken included
  - calculate is_flare_friendly set correctly
  - calculate missing_ingredients listed
    when food_id not found
  - create saves meal and ingredients
  - create logs missing ingredient names
  - get_custom_meals returns only user's meals
  - delete removes meal and ingredients
  - delete other user's meal returns 403

Write router tests:
tests/routers/test_custom_meals.py
  - POST /calculate returns 200 no DB write
  - POST /meals/custom creates and returns meal
  - GET /meals/custom returns user's meals only
  - GET /meals/custom/{id} returns full meal
  - GET /meals/custom/{other_user_id} → 404
  - DELETE /meals/custom/{id} → 200
  - All endpoints return 401 without token

Print: ✓ TASK 5 COMPLETE

───────────────────────────────────────────────────────────
TASK 6 — USER PROFILE ENDPOINTS
───────────────────────────────────────────────────────────

Write: app/routers/profile.py

These endpoints are needed by the
Week 7 onboarding flow.

GET /api/v1/profile
  Auth: required
  Response: full UserProfileResponse
    user: UserResponse
    preferences: UserPreferencesResponse
    medications: list[UserMedicationResponse]

PUT /api/v1/profile/preferences
  Auth: required
  Body: UserPreferencesUpdate
    dietary_flags: list[str] | null
      values: vegetarian, vegan,
              gluten_free, dairy_free,
              nut_free, halal, kosher
    allergies: list[str] | null
    goals: list[str] | null
      values: reduce_inflammation,
              lose_weight, maintain_weight,
              increase_energy, better_sleep
    cuisine_preferences: list[str] | null
    budget_friendly: bool | null
  Response: UserPreferencesResponse

POST /api/v1/profile/medications
  Auth: required
  Body: UserMedicationCreate
    medication_name: str
    dosage: str | null
    frequency: str | null
  Response: UserMedicationResponse

DELETE /api/v1/profile/medications/{id}
  Auth: required
  Response: { status: "removed" }
  404 if not found or not owned by user

Write service: app/services/profile_service.py
  - get_full_profile(db, user_id)
  - update_preferences(db, user_id, data)
  - add_medication(db, user_id, data)
  - remove_medication(db, user_id, medication_id)

Register in app/main.py

Write tests: tests/routers/test_profile.py
  - GET /profile returns user + prefs + meds
  - PUT /preferences updates dietary_flags
  - PUT /preferences updates allergies
  - POST /medications adds medication
  - DELETE /medications removes medication
  - DELETE /medications other user → 404
  - All endpoints return 401 without token

Print: ✓ TASK 6 COMPLETE

───────────────────────────────────────────────────────────
TASK 7 — API CONTRACT DOCUMENT
───────────────────────────────────────────────────────────

Write: docs/api_contract.md

This is the source of truth for Week 7
React frontend. Every endpoint the frontend
will call must be documented here.

For every endpoint write:

Method + path:
Auth required: yes/no
Query params: (if any)
Request body: (if any)
Response shape: (exact field names and types)
Error responses: (status codes and when)
Notes: (any frontend-specific behaviour)

Group by module:

1. Auth
   POST /api/v1/auth/register
   POST /api/v1/auth/login
   POST /api/v1/auth/refresh
   POST /api/v1/auth/logout
   GET  /api/v1/auth/me
   PUT  /api/v1/auth/me
   POST /api/v1/auth/change-password

2. Profile
   GET  /api/v1/profile
   PUT  /api/v1/profile/preferences
   POST /api/v1/profile/medications
   DELETE /api/v1/profile/medications/{id}

3. Foods
   GET /api/v1/foods/search
   GET /api/v1/foods/categories
   GET /api/v1/foods/{food_id}

4. Meals (curated)
   GET /api/v1/meals
   GET /api/v1/meals/flare-safe
   GET /api/v1/meals/by-tag/{tag}
   GET /api/v1/meals/{meal_id}

5. Custom meals
   POST /api/v1/meals/custom/calculate
   POST /api/v1/meals/custom
   GET  /api/v1/meals/custom
   GET  /api/v1/meals/custom/{meal_id}
   DELETE /api/v1/meals/custom/{meal_id}

6. Ingredients
   POST /api/v1/ingredients/search
   POST /api/v1/ingredients/missing
   GET  /api/v1/ingredients/missing (admin)
   PATCH /api/v1/ingredients/missing/{id} (admin)

7. Recommendations
   GET  /api/v1/recommendations/next
   POST /api/v1/recommendations/{id}/feedback
   GET  /api/v1/recommendations/history

8. Logs
   POST /api/v1/logs/food
   GET  /api/v1/logs/food/today
   GET  /api/v1/logs/food/{date}
   DELETE /api/v1/logs/food/{log_id}
   POST /api/v1/logs/symptoms
   GET  /api/v1/logs/symptoms/today
   GET  /api/v1/logs/symptoms/{date}
   POST /api/v1/logs/lifestyle
   GET  /api/v1/logs/lifestyle/today

9. Dashboard
   GET /api/v1/dashboard/today
   GET /api/v1/dashboard/weekly

Also document:

Authentication flow for frontend:
  1. POST /auth/login → store access_token
     and refresh_token in memory
     (never localStorage — not supported)
  2. Add header to every request:
     Authorization: Bearer {access_token}
  3. On 401 response:
     try POST /auth/refresh with refresh_token
     if refresh succeeds → retry original request
     if refresh fails → redirect to login

Error response shape (all endpoints):
  {
    "detail": "human readable message",
    "code": "machine readable code"
  }

Medical escalation response shape:
  When symptom log triggers escalation:
  {
    "escalation_triggered": true,
    "escalation_message": "Your symptoms sound
      severe. Please contact your rheumatologist
      or seek medical advice. This app provides
      lifestyle support only."
  }

Print: ✓ TASK 7 COMPLETE

───────────────────────────────────────────────────────────
TASK 8 — FULL TEST SUITE RUN
───────────────────────────────────────────────────────────

Run complete test suite:
  pytest tests/ -v

Target: 80+ tests passing

Fix any failing tests before proceeding.

Also run a full smoke test covering
the new Week 6 endpoints:
Write: scripts/week6_smoke.py

Flow to test:

Step 1: Register new user
Step 2: Login → get tokens
Step 3: Set preferences:
  vegetarian: true
  allergies: []
  goals: [reduce_inflammation]
Step 4: Add medication: "Methotrexate 10mg"
Step 5: Search ingredient: "spinach"
  → expect: found in static.foods
Step 6: Search ingredient: "dragonfruit"
  → expect: not found
Step 7: Submit missing ingredient "dragonfruit"
  name only → expect: missing_completely
Step 8: Submit missing ingredient "papaya"
  with calories: 43, protein: 0.5,
  fiber: 1.7
  → expect: user_entered
Step 9: Build custom meal calculate:
  chicken breast 150g + spinach 80g
  + olive oil 10g
  → expect: nutrients calculated
  → expect: no DB write
Step 10: Save custom meal:
  "My protein lunch"
  → expect: saved to tracking.custom_meals
Step 11: Log custom meal as food log:
  log_source: custom_meal
  → expect: raw values stored
Step 12: Log symptoms:
  pain_score: 7, flare_level: moderate
  → expect: flare_only recommendation mode
Step 13: Get recommendation:
  meal_type: dinner
  → expect: recommendation_mode = flare_only
  → expect: all results is_flare_friendly
Step 14: Log symptoms:
  pain_score: 4, flare_level: mild
  → expect: mixed recommendation mode
Step 15: Get recommendation:
  meal_type: lunch
  → expect: recommendation_mode = mixed
  → expect: first 2 results flare_friendly
  → expect: results 3–5 not flare_friendly
Step 16: Log symptoms:
  pain_score: 2, flare_level: none
  → expect: normal recommendation mode
Step 17: Get weekly dashboard
  → expect: data present

Save smoke output to:
logs/week6_smoke_{timestamp}.json

Print: ✓ TASK 8 COMPLETE

───────────────────────────────────────────────────────────
TASK 9 — VERIFICATION QUERIES
───────────────────────────────────────────────────────────

Run and save to logs/week6_verification.txt:

Query 1 — missing_ingredients table exists:
  SELECT COUNT(*) FROM tracking.missing_ingredients;
  → expect: 2 (dragonfruit + papaya from smoke)

Query 2 — status breakdown:
  SELECT status, COUNT(*)
  FROM tracking.missing_ingredients
  GROUP BY status;
  → expect:
    missing_completely: 1 (dragonfruit)
    user_entered: 1 (papaya)

Query 3 — custom meal saved:
  SELECT name, total_calories,
         anti_inflammatory_score,
         is_vegetarian
  FROM tracking.custom_meals
  ORDER BY created_at DESC
  LIMIT 5;

Query 4 — recommendation mode logged:
  SELECT recommendation_mode, COUNT(*)
  FROM intelligence.recommendation_logs
  GROUP BY recommendation_mode;
  → expect flare_only and mixed present

Query 5 — food log source breakdown:
  SELECT log_source, COUNT(*)
  FROM tracking.food_logs
  GROUP BY log_source;
  → expect custom_meal present

Query 6 — new foods column:
  SELECT COUNT(*) FROM static.foods
  WHERE needs_review = true;
  → expect 0 (no user-submitted foods yet)

Print: ✓ TASK 9 COMPLETE

───────────────────────────────────────────────────────────
TASK 10 — WRITE WEEK 6 SUMMARY
───────────────────────────────────────────────────────────

Write week6_summary.md covering:

1. Migration 0006 changes
   - missing_ingredients table
   - needs_review column on foods
   - recommendation_mode column on logs

2. Rule engine update
   - pain-driven mode implemented
   - three modes confirmed: normal, mixed,
     flare_only
   - mixed mode verified with test fixture

3. Auth flow review
   - endpoints confirmed and tested
   - token rotation confirmed
   - security rules verified

4. Missing ingredients system
   - two status paths: missing_completely
     and user_entered
   - reported_count deduplication
   - admin review endpoints

5. Custom meal builder
   - calculate endpoint (no DB write)
   - save endpoint
   - log_source = custom_meal chain verified

6. Profile endpoints
   - preferences, medications confirmed

7. API contract document location
   - docs/api_contract.md

8. Test results
   - total tests passing

9. Open questions for Week 7
   - which screens to build first?
   - mobile-first or desktop-first?
   - any API shape changes needed?

10. What Week 7 needs
    - docs/api_contract.md as source of truth
    - confirmed auth flow
    - confirmed token storage strategy
      (in-memory, not localStorage)

═══════════════════════════════════════════════════════════
EXECUTION RULES
═══════════════════════════════════════════════════════════

DB CONNECTION:
- Use existing get_db_session() from app/db/session.py
- Do NOT rewrite connection logic
- Async generator pattern only

DO NOT OVERWRITE:
- app/db/session.py
- app/core/config.py
- app/services/food_normalizer.py
- app/services/anti_inflammatory_scorer.py
- app/routers/foods.py
- app/routers/meals.py
- app/routers/logs.py (extend only — do not rewrite)
- app/services/rule_engine.py (update only —
  do not rewrite entire file)
- alembic/versions/0001 through 0005

DO NOT BUILD:
- React frontend (Week 7)
- ML models (Week 15+)
- Admin UI (future)

MEDICAL SAFETY:
- All medication filter rules must keep
  [VERIFY WITH CLINICIAN] comments
- Escalation message must appear in
  symptom log response when triggered
- Medical disclaimer must appear in
  API contract document

GENERAL:
- Complete every task fully before moving
  to next
- Write and save all files to disk
- Run scripts inside Docker API container
- Run queries inside Docker DB container
- Flag decisions with [DESIGN DECISION: ...]
- At end of each task: ✓ TASK [N] COMPLETE
- When all done print:
  ✓ WEEK 6 COMPLETE — auth locked,
  custom meals live, missing ingredients
  system built, API contracts documented,
  Week 7 frontend ready to begin

Begin now with Task 1.